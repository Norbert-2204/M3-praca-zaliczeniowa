version: "3.8"

services:
  postgres:
    image: postgres:15-alpine
    container_name: node_sql_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-mydb}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - node_sql_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: node_sql_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@admin.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_CONFIG_SERVER_MODE: "False"
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - node_sql_network
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
  pgadmin_data:

networks:
  node_sql_network:
    driver: bridge
# version: "3.9"

# services:
#   app:
#     build: .
#     container_name: Devstock-hub
#     ports:
#       - "3000:3000"
#     environment:
#       DATABASE_URL: ${DATABASE_URL}
#     volumes:
#       - .:/usr/src/app
#       - /usr/src/app/node_modules
#     depends_on:
#       postgres:
#         condition: service_healthy

#     # =========================
#     # DEV MODE (hot reload)
#     # =========================
#     # command: npm run dev

#     # =========================
#     # PROD MODE (build + start)
#     # =========================
#     command: sh -c "npm run build && npm run start"
#     networks:
#       - node_sql_network

#   postgres:
#     image: postgres:15-alpine
#     container_name: node_sql_postgres
#     environment:
#       POSTGRES_USER: ${POSTGRES_USER:-postgres}
#       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
#       POSTGRES_DB: devstockhub
#     ports:
#       - "${POSTGRES_PORT:-5432}:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     networks:
#       - node_sql_network
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

# volumes:
#   postgres_data:

# networks:
#   node_sql_network:
# version: "3.9"

# services:
#   app:
#     build: .
#     ports:
#       - "3000:3000"
#     environment:
#       DATABASE_URL: ${DATABASE_URL}
#     depends_on:
#       postgres:
#         condition: service_healthy
#     networks:
#       - node_sql_network

#   postgres:
#     image: postgres:15-alpine
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: devstockhub
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     networks:
#       - node_sql_network
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

# volumes:
#   postgres_data:

# networks:
#   node_sql_network:
